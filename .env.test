# Loaded by next/jest for `npm run test` (see https://nextjs.org/docs/pages/building-your-application/testing/jest#setting-up-jest-with-the-rust-compiler).
# Dummy values only — the Supabase client itself is mocked in tests.
NEXT_PUBLIC_SUPABASE_URL=https://test.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key
